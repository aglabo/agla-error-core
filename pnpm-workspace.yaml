# src; pnpm-workspace.yaml
# @(#) : monorepo defintion with pnpm
packages:
  - packages/**
allowBuilds:
  esbuild: true
  unrs-resolver: set this to true or false

# security: force patched transitive versions (Dependabot #38, #44)
overrides:
  postcss: ^8.5.26
  esbuild: ^0.28.2
